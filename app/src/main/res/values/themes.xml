<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.AlphaChat" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/my_primary</item>
        <item name="android:navigationBarColor">@color/my_primary</item>
        <!-- Optional: Disable contrast enforcement if you want full transparency -->
        <item name="android:enforceStatusBarContrast">false</item>
    </style>

    <style name="Theme.AlphaChat" parent="Base.Theme.AlphaChat" />
</resources>